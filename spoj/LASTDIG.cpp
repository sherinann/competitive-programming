#include <iostream>
#include <bitset>
using namespace std;
int main(){
	//ios_base::sync_with_stdio(false);
	int t,a;
	long long b,r;
	cin>>t;
	while(t--){
		cin>>a>>b;
		char x[32];
		cout<<bitset<8>(a);
		
	}
}